# Use node-linker=hoisted for better compatibility with Cloudflare Pages
node-linker=hoisted
# Ensure public-hoist-pattern includes cookie
public-hoist-pattern[]=cookie
# Ensure dependencies are properly resolved
shamefully-hoist=true
# Ensure strict peer dependencies are not enforced
strict-peer-dependencies=false
