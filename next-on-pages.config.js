/**
 * Configuration for @cloudflare/next-on-pages
 * https://github.com/cloudflare/next-on-pages
 */
export default {
  // External packages that should not be bundled
  externals: ['cookie'],
  
  // Optional: Additional configuration for the build process
  buildConfig: {
    // You can add more build-specific configuration here if needed
  }
};
